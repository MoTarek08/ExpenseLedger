# name: Main PR CI

# on:
#   pull_request:
#     branches:
#       - main
#     types:
#       - opened
#       - reopened
#       - synchronize

# env:
#   REGISTRY: ghcr.io
#   IMAGE_NAME: ghcr.io/${{ github.repository }}
#   DOTNET_VERSION: "10.0.x"

# jobs:

#   # ---------------------------------------------------------
#   # Only dev and bug-fix/* are allowed to target main.
#   # Make this check required in your main branch ruleset.
#   # ---------------------------------------------------------
#   validate-source-branch:
#     name: Validate source branch
#     runs-on: ubuntu-latest

#     steps:
#       - name: Validate
#         shell: bash
#         run: |
#           branch="${{ github.head_ref }}"

#           if [[ "$branch" == "dev" || "$branch" == bug-fix/* ]]; then
#             echo "Allowed source branch: $branch"
#           else
#             echo "Invalid source branch: $branch"
#             exit 1
#           fi


#   # ---------------------------------------------------------
#   # Your normal source-level CI
#   # ---------------------------------------------------------
#   tests:
#     name: Tests
#     needs: validate-source-branch
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v6

#       - name: Setup .NET
#         uses: actions/setup-dotnet@v5
#         with:
#           dotnet-version: ${{ env.DOTNET_VERSION }}

#       - name: Restore
#         run: dotnet restore

#       - name: Unit tests
#         run: dotnet test tests/UnitTests --configuration Release

#       - name: Integration tests
#         run: dotnet test tests/IntegrationTests --configuration Release


#   # ---------------------------------------------------------
#   # Build the actual production-shaped Docker image
#   # ---------------------------------------------------------
#   docker-build-and-system-test:
#     name: Docker system tests
#     needs: tests
#     runs-on: ubuntu-latest

#     permissions:
#       contents: read

#     steps:
#       - uses: actions/checkout@v6

#       - name: Build image
#         run: |
#           docker build \
#             --file Dockerfile \
#             --tag expenseledger:test \
#             .

#       - name: Start application
#         run: |
#           docker compose \
#             -f docker-compose.ci.yml \
#             up -d

#       - name: Wait for application
#         shell: bash
#         run: |
#           for i in {1..60}; do
#             if curl --fail --silent http://localhost:8080/health; then
#               echo "Application is healthy"
#               exit 0
#             fi

#             sleep 2
#           done

#           echo "Application failed to become healthy"
#           docker compose -f docker-compose.ci.yml logs
#           exit 1

#       # These are your deeper black-box/system tests.
#       # They talk to the running Dockerized application.
#       - name: Run system tests
#         run: |
#           dotnet test tests/SystemTests \
#             --configuration Release \
#             --environment TestBaseUrl=http://localhost:8080

#       - name: Shutdown
#         if: always()
#         run: |
#           docker compose \
#             -f docker-compose.ci.yml \
#             down -v


#   # ---------------------------------------------------------
#   # Only after ALL validation passes:
#   # push candidate image to GHCR.
#   # ---------------------------------------------------------
#   publish-candidate:
#     name: Publish candidate image
#     needs: docker-build-and-system-test
#     runs-on: ubuntu-latest

#     permissions:
#       contents: read
#       packages: write

#     steps:
#       - uses: actions/checkout@v6

#       - name: Log in to GHCR
#         uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Build candidate image
#         run: |
#           docker build \
#             --file Dockerfile \
#             --tag "$IMAGE_NAME:pr-${{ github.event.pull_request.number }}-${{ github.event.pull_request.head.sha }}" \
#             .

#       - name: Push candidate image
#         run: |
#           docker push \
#             "$IMAGE_NAME:pr-${{ github.event.pull_request.number }}-${{ github.event.pull_request.head.sha }}"