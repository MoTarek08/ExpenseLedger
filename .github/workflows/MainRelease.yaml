# name: Main Release

# on:
#   pull_request:
#     branches:
#       - main
#     types:
#       - closed

# env:
#   REGISTRY: ghcr.io
#   IMAGE_NAME: ghcr.io/${{ github.repository }}

# jobs:

#   release:
#     # Only continue when the PR was actually merged.
#     if: github.event.pull_request.merged == true

#     runs-on: ubuntu-latest

#     permissions:
#       contents: read
#       packages: write

#     outputs:
#       image: ${{ steps.image.outputs.image }}

#     steps:

#       - name: Log in to GHCR
#         uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       # ---------------------------------------------
#       # Reconstruct the exact candidate tag
#       # ---------------------------------------------
#       - name: Determine candidate
#         id: candidate
#         shell: bash
#         run: |
#           PR_NUMBER="${{ github.event.pull_request.number }}"
#           HEAD_SHA="${{ github.event.pull_request.head.sha }}"

#           CANDIDATE="$IMAGE_NAME:pr-${PR_NUMBER}-${HEAD_SHA}"

#           echo "candidate=$CANDIDATE" >> "$GITHUB_OUTPUT"

#           echo "Candidate image:"
#           echo "$CANDIDATE"


#       # ---------------------------------------------
#       # Pull the EXACT image that passed PR CI
#       # ---------------------------------------------
#       - name: Pull candidate
#         run: |
#           docker pull "${{ steps.candidate.outputs.candidate }}"


#       # ---------------------------------------------
#       # Resolve the immutable Docker digest
#       # ---------------------------------------------
#       - name: Resolve digest
#         id: image
#         shell: bash
#         run: |
#           CANDIDATE="${{ steps.candidate.outputs.candidate }}"

#           REPO_DIGEST=$(docker image inspect "$CANDIDATE" \
#             --format='{{index .RepoDigests 0}}')

#           DIGEST="${REPO_DIGEST##*@}"

#           echo "Candidate digest:"
#           echo "$DIGEST"

#           echo "image=$IMAGE_NAME@$DIGEST" >> "$GITHUB_OUTPUT"
#           echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"


#       # ---------------------------------------------
#       # Promote the SAME image.
#       #
#       # No docker build here.
#       # ---------------------------------------------
#       - name: Promote release image
#         run: |
#           CANDIDATE="${{ steps.candidate.outputs.candidate }}"
#           RELEASE="$IMAGE_NAME:main-${{ github.event.pull_request.merge_commit_sha }}"

#           docker tag "$CANDIDATE" "$RELEASE"

#           docker push "$RELEASE"

#           echo "Promoted:"
#           echo "$RELEASE"


#       - name: Verify release digest
#         shell: bash
#         run: |
#           RELEASE="$IMAGE_NAME:main-${{ github.event.pull_request.merge_commit_sha }}"

#           docker pull "$RELEASE"

#           RELEASE_REPO_DIGEST=$(docker image inspect "$RELEASE" \
#             --format='{{index .RepoDigests 0}}')

#           RELEASE_DIGEST="${RELEASE_REPO_DIGEST##*@}"

#           EXPECTED="${{ steps.image.outputs.digest }}"

#           echo "Expected: $EXPECTED"
#           echo "Release:  $RELEASE_DIGEST"

#           test "$EXPECTED" = "$RELEASE_DIGEST"

#           echo "Exact image verified."